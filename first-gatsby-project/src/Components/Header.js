import { Link } from "gatsby";
import React from "react";

const Header = ({ daysOfWeek }) => {
  return (
    <header className={"header layout-content-container"}>
      <h1>Days of the Week</h1>
    </header>
  );
};

export default Header;
